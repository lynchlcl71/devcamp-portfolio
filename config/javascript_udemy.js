function test()
{
  return 5;
}

var a = test();

alert(a);

-----------------------------------------------------------
function add(x,y)
{
  return x + y;
}

var sumOfVariables = add(1,3);

alert(sumOfVariables);

-------------------------------------------------------------

function divide(x,y)
{
  if (y === 0)
    return "You can't divide by Zero!!!"

    return x / y
}

var result  = divide(10,5);

alert(result);

---------------------------------------------------------------